config {
  type: "table",
  schema: "staging",
  description: "About to do some aggregration stuff",
  assertions: {
    nonNull: ["country"]
  }
  
}
select country, sum(cast(score as int)) as sum_country_score
from ${ref("people_scores")}
group by country
order by sum_country_score desc
